<section class="trust-badges-section">
  <div class="trust-badges">
    <div class="trust-badge">
      <div class="trust-badge-icon">🚚</div>
      <h4>{{ 'sections.trust.free_shipping' | t }}</h4>
      <p>{{ 'sections.trust.free_shipping_desc' | t }}</p>
    </div>
    <div class="trust-badge">
      <div class="trust-badge-icon">↩️</div>
      <h4>{{ 'sections.trust.easy_returns' | t }}</h4>
      <p>{{ 'sections.trust.easy_returns_desc' | t }}</p>
    </div>
    <div class="trust-badge">
      <div class="trust-badge-icon">✓</div>
      <h4>{{ 'sections.trust.quality' | t }}</h4>
      <p>{{ 'sections.trust.quality_desc' | t }}</p>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Trust Badges",
  "settings": [],
  "presets": [
    {
      "name": "Trust Badges"
    }
  ]
}
{% endschema %}
